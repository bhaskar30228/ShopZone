
      <% if(errors.length>0){ %>
            <div>
              <strong>Error!</strong>
              <span>
                <% errors.forEach(error=>{ %>
                  <li><%= error %></li>
               <% }) %>
              </span>
            </div>
       <% } %>